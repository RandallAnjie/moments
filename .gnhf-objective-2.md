下面 6 个独立的前端 UX 问题需要修。每条都包含问题描述、可能位置、修法思路、完成判据。请按从易到难（6 → 1 → 4 → 5 → 3 → 2）的顺序做，每条做完写 notes、commit、build 通过，再做下一条。

# 工程约束（全局）

- 这是一个 Nuxt 3 + Vue 3 + Tailwind / shadcn-vue 项目，部署目标是 Cloudflare Pages（Workers Runtime）。SSR 必须能 build 通过（`pnpm build`）；任何引入 Node-only API 的依赖都会破坏 build。
- 服务端代码不要改（D1 / KV / R2 / 认证那一套已经稳定，跑在生产）。这一轮所有改动应该集中在：`components/`、`pages/`、`composables/`、`layouts/`、`public/`、`nuxt.config.ts`、`tailwind.config.ts`。
- 每个 iteration 只动一个问题。每次 commit 前必须 `pnpm build` 通过，commit message 用中文。

# 问题 1：功能按钮在多个的情况下换行成两行

**现象**：memo 卡片或编辑器某个工具栏（可能是 `MemoInput.vue` 或 memo 详情 `OnesMemo.vue` / `FriendsMemo.vue`）有一排"功能按钮"（比如位置、图片、音乐、bilibili、外链…），按钮多的时候中文 label 会把一个按钮压成换行内部两行，比 layout 行高还高，看上去乱。

**修法思路**：找到这个工具栏。

- 如果它本来期望横向滚动 / 紧凑：让 button 用 `whitespace-nowrap`，外层用 `overflow-x-auto`
- 如果空间真的不够：当容器宽度 < 某阈值时，改成每行一个按钮（垂直 stack）。可以用 `flex-wrap` + `min-w-full` on each child，或者基于断点切 `flex-col`
- 优先方案：**每行一个按钮**（用户明确要求）。所以把那个工具栏改成纵向布局，按钮 `w-full` 左对齐，图标 + 文字横排

**完成判据**：
- 视觉上：在常见手机宽度（360 / 414px）下，所有功能按钮一行一个，文字不会被压换行
- 不影响桌面端外观（桌面也按用户要求每行一个；或保持横向滚动）

# 问题 2：无限滚动（virtualized timeline）

**现象**：现在首页 `pages/index.vue` 应该是一次性渲染所有已加载的 memo（或分页加载但 DOM 累积）。memo 多了 DOM 节点上千，移动端卡。

**目标**：像 x.com / 微博：

- DOM 里只渲染当前视口附近的几条（比如 visible + 上下各 3 个 buffer）
- 滚动到底部时自动加载下一页（替换或追加）
- 已划过的 memo 从 DOM 移除（保留状态在 store / 数据数组里）
- 总高度通过虚拟撑开保持滚动条比例正确

**修法思路**：

- 推荐用 `@tanstack/vue-virtual` 或 `vueuse/core` 的 `useVirtualList`。前者更专业，后者依赖更轻
- 已有的 `getMore` IntersectionObserver / 「加载更多」按钮可以保留作为兜底
- 配合问题 3，需要保存当前已加载的所有 memo 数据 + 当前虚拟滚动的偏移量

**完成判据**：
- DevTools 里 memo 节点总数始终 ≤ 30（不管已经"加载"了多少条）
- 滚动到底自动拉下一页
- 滚动很顺，不闪烁

# 问题 3：返回保持滚动位置

**现象**：从首页点进 memo 详情（`/d/:id` 或类似），按浏览器返回，首页滚回顶部。

**目标**：返回时回到点进去之前的滚动位置（包括虚拟列表里加载的所有页 + 当时的 scrollTop）。

**修法思路**：

- Nuxt 默认会做基础滚动恢复，但对 SPA route 不一定生效（特别是异步加载的数据）
- 用 `defineRouteRules` / `definePageMeta({ scrollToTop: false })` + Pinia store 缓存首页状态：
  - 已加载的 memo 数组
  - 当前虚拟列表的 scrollTop
  - 当前页号 / hasMore
- 切换 route 离开首页时把状态存进 store；回到首页时如果 store 有缓存，跳过初始 fetch，直接渲染缓存 + 恢复 scrollTop
- 区分"首次访问首页"和"返回首页"：用 `router.options.scrollBehavior` 里的 `savedPosition` 或者 `useRouter().forward / back` 事件

**完成判据**：
- 首页滚到第 50 条 → 点进详情 → 返回 → 还在第 50 条
- 直接刷新首页（F5）→ 仍然从顶端开始（不能错误地恢复历史 scrollTop）

# 问题 4：顶部头图占位 + 缓存避免布局跳动

**现象**：每次进入页面，顶部头图（`components/HeaderImg.vue`）加载之前是 0 高度，加载完撑开容器，导致下方内容跳一下（CLS）。

**目标**：

- 加载前就用 aspect-ratio 占位（基于已知比例，例如 16:9 或具体像素）
- 用 `<img loading="eager" fetchpriority="high">` 让头图最先加载
- 配合浏览器 HTTP 缓存（设置正确的 Cache-Control）；R2 桶上的图片走 worker `/upload/<file>` 时已经有 `cache-control: public, max-age=31536000, immutable`，确认这个还在
- 头图 URL 可以 prefetch（`<link rel="preload" as="image" href="...">`）

**修法思路**：

- 改 `components/HeaderImg.vue`：给容器 `aspect-[3/1]` 或类似的 Tailwind class，让它在图加载前就有正确高度
- 用 store 或 `useState` 缓存 user/settings 拉到的 coverUrl，避免每次组件 mount 重新 fetch
- 进一步用 PWA / service-worker 缓存图片资源（和问题 5 联动）

**完成判据**：
- 首次访问页面，头图区域从一开始就占位（视觉上没有空白 → 撑开的跳动）
- 二次刷新页面，头图从浏览器/SW 缓存秒出

# 问题 5：PWA 支持

**目标**：加 PWA 让用户"添加到主屏幕"，离线时能看缓存内容、安装为 standalone webapp。

**修法思路**：

- 用 `@vite-pwa/nuxt` 模块（标准方案）；配置 manifest（name、short_name、theme_color、background_color、display: standalone、icons）+ Workbox runtime caching
- 准备一组 icon（192 / 256 / 384 / 512 png）放在 `public/`，可以基于 `public/r-logo.png` / `public/favicon.png` 生成或先用 placeholder
- Service worker 策略：
  - 静态资源（`/_nuxt/*` / `/css/*` / `/js/*`）→ CacheFirst
  - 头图 / 上传图片（`/upload/*` + R2 公开 URL）→ StaleWhileRevalidate
  - HTML 文档 → NetworkFirst（这样有更新能及时看到）
- 确保 `manifest.webmanifest` 和 sw 文件能从 Pages 静态目录直接 serve，不进 worker

**完成判据**：
- Chrome 地址栏出现 "安装" 提示，安装后能 standalone 启动
- Lighthouse PWA 评分 ≥ 80
- 离线状态下首页能渲染（fallback 到 SW 缓存）

# 问题 6：「全文 / 收起」按钮失效

**现象**：原本长 memo 内容超过一定高度时显示"全文"按钮，点开展开；展开后显示"收起"。最近 regression 后失效。

**位置**：`components/FriendsMemo.vue` 第 11-12 行（`hh > 96 && !showAll` / `showAll`）。`hh` 应该是某个 ref 跟踪内容高度。

**修法思路**：

- 检查 `hh` 的赋值来源 —— 大概率是个 `nextTick` 之后 `ref(element).offsetHeight`，但因为某次重构（可能去掉 web-socket / atpeople 渲染那一段时）DOM 结构变了，导致测量的不是真正的 content 元素
- 或者 `showAll` 状态切换后样式没正确切换（max-height / line-clamp）
- 修复后确认：
  - 短 memo（< 96px）不出现"全文"
  - 长 memo 出现"全文"，点击后展开全部内容，按钮变"收起"
  - 再点"收起"折叠回去

# 完成判据（gnhf stop-when）

当所有 6 项都对应有 commit、`pnpm build` 通过、且 commit message 明确说"已修复问题 N"覆盖到 1～6 全部时，报告 `UX_FIXES_COMPLETE`。

# 工作建议

- 每条做完后 push 到 cloudflare-migration（gnhf 设置了 --push）
- notes.md 里记录：每个问题的具体根因 / 改动的文件清单 / 任何遗留
- 不要重构和本任务无关的代码
- 不要破坏 Cloudflare 兼容性：禁止用 Node-only 包；只能用 Web Crypto / fetch / Cloudflare 兼容的库
