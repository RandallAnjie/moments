将这个 Nuxt 3 应用完全迁移到 Cloudflare 平台，使其 100% 依赖 Cloudflare 服务部署，不再需要任何外部基础设施。

## 当前栈
- Nuxt 3 (SSR) + Vue 3 + Pinia + Tailwind/shadcn
- Prisma ORM + MySQL（schema.prisma 用了 `@db.Text` 等 MySQL-only 类型）
- 图片存储：@aws-sdk/client-s3 + minio
- 缓存/限流：ioredis + redis + express-rate-limit
- 邮件：nodemailer
- 实时：WebSocket（wscat / worktop 依赖）
- 密码：bcrypt（native binding）
- JWT：jsonwebtoken
- Docker 部署（Dockerfile / docker-compose / install.sh / start.sh）

## 迁移目标

1. **部署**：Cloudflare Pages（用 Nuxt 的 `cloudflare-pages` nitro preset）；提供完整的 `wrangler.toml`，包含 D1、R2、KV、（必要时）Durable Object 绑定
2. **数据库**：Prisma + MySQL → Cloudflare D1（SQLite）。推荐用 drizzle-orm + better-sqlite3 本地开发 / D1 binding 生产；schema 需要适配 SQLite（去掉 `@db.Text`，bool 用 int 等）；提供 migration SQL 文件 + 一个简单的 seed 脚本
3. **对象存储**：S3 / Minio → Cloudflare R2 binding；上传走 Worker，读取走 R2 public bucket 或自定义域名
4. **邮件**：nodemailer → MailChannels（Workers 上免费的 fetch 调用，`api.mailchannels.net/tx/v1/send`）；保留发件人配置项
5. **缓存/限流**：移除 ioredis / redis / `Dockerfile_with_redis` 系列；若需要限流就用 Workers 自身的 KV 或 Durable Object，否则去掉
6. **WebSocket**：用 Cloudflare Durable Objects 重写（hibernation API）；如果实现成本高，可先去掉 WebSocket，标记为后续工作
7. **密码哈希**：bcrypt → Web Crypto PBKDF2（或 `bcryptjs` 纯 JS 版本），需要写一次性迁移函数把已有 bcrypt hash 兼容验证 + 重哈希
8. **JWT**：`jsonwebtoken` → `@tsndr/cloudflare-worker-jwt` 或 `jose`（Web Crypto 兼容）
9. **图像/敏感词检测**：阿里云 detective 相关代码若依赖 Node 特性，改成 fetch 调用，或保留为可选 feature flag
10. **管理后台/配置**：Config 表里的 `s3*` 字段保留兼容，新增 `r2*` 字段或复用；管理后台 UI 同步更新
11. **资源清理**：删除 `Dockerfile*`、`docker-compose*.yaml`、`install.sh`、`start*.sh`、`prisma/`（迁移到新的 db 目录）、`.env` 提交（改用 `wrangler.toml` + `.dev.vars` 模板）；更新 `.gitignore`
12. **文档**：完全重写 README，只说 Cloudflare 部署（前置：Cloudflare 账号 / wrangler CLI / 创建 D1+R2+KV 步骤 / `pnpm wrangler d1 migrations apply` / `pnpm wrangler pages deploy` / 环境变量列表）
13. **package.json**：删除不再需要的 dep（@aws-sdk/*、minio、ioredis、redis、bcrypt、nodemailer、jsonwebtoken、worktop、wscat、prisma、@prisma/client、@alicloud/pop-core 若不需要、express-rate-limit）；新增 wrangler / drizzle-orm / @cloudflare/workers-types / @tsndr/cloudflare-worker-jwt 等

## 工作原则

- 每次迭代只做一件小事（一个模块、一个表、一个 API endpoint）
- 每次迭代后 `pnpm install` + `pnpm build` 必须通过；类型检查不能退步
- 用 `notes.md` 记录已完成 / 待办 / 设计决策（D1 schema 设计、R2 路径规则、密码迁移策略等）
- 业务代码（pages/、components/）尽量保留不变，主要重写 server/、lib/、prisma/、services/、composables/ 中和后端相关的部分
- 安全：不要把任何真实密钥提交到仓库；wrangler 配置里只放占位符，写在 README 里
- 提交信息用中文，沿用现有仓库风格

## 完成判据（gnhf stop-when 会用到）

当满足以下全部条件时报告 `MIGRATION_COMPLETE`：
- `pnpm install && pnpm build` 干净通过
- 仓库中无 prisma / @prisma / mysql / ioredis / redis / bcrypt（native）/ nodemailer / @aws-sdk / minio / Dockerfile* / docker-compose* / install.sh / start.sh 的引用
- 存在 `wrangler.toml` + `migrations/` 目录 + 新 README
- `pnpm wrangler pages dev` 或等价命令能本地启动并响应基本路由（agent 应自己验证）
- 关键 API（登录、发 memo、上传图片、评论）的代码路径已经走 D1 + R2 + 新密码方案
- 删除了 `.env` 中的明文敏感信息，提供 `.dev.vars.example`
